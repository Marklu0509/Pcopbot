apiVersion: 1

# Provision the Prometheus data source with a fixed UID so alert rules
# can reference it reproducibly (instead of a random UID from the UI).
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
